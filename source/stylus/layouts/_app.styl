/* ==========================================================================
   -- App
   ========================================================================== */

#app
    display flex
    flex-direction row
    min-height 100%

    > #main-container
        display flex
        flex-direction column

        > #router-outlet
            display flex
            flex-direction column
            flex-grow 1